# Build with remote backend API URL.
# Copy to .env.production before: npm run build
#
# Same-origin (frontend + API on one server via Nginx): use /api
# Or full URL if the UI is hosted elsewhere: e.g. http://YOUR_EC2_IP/api

VITE_API_URL=/api
VITE_WS_URL=
